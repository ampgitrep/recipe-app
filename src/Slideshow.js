import { Container, Column } from "bulma";
import { useState } from "react";

const SlideShow = ({ recipeList }) => {
  const [currentSlide, setCurrentSlide] = useState('');

  const prevSlide = () => {

  }

  const nextSlide = () => {
    
  }
}
